<h1>Burgers</h1>

<h2>All Burgers on the menu</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/cat-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that you've eaten</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/cat-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger:</label>
    <input type="text" id="ca" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="eaten">eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> eaten!<br>
    <input type="radio" name="eaten" value="0"> Awake
  </div> --}}

  <button type="submit">Add Burger</button>
</form>
